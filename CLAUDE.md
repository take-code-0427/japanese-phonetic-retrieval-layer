# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクトの目的

日本語の「音韻空間」を検索レイヤーとして提供する (Phonetic RAG)。semantic embedding が
意味の近さを引くのに対し、ここは **発音の近さ** だけを引く。意味と音は独立した軸として
扱い、混ぜない。音韻検索は候補を出すところまでを担い、意味による最終選択は呼び出し側
(LLM) が行う — この分担が設計上の中心的な前提。詳細な背景と実測値は `README.md`。

## コマンド

```bash
uv sync --extra full                       # Rust 拡張もここでビルドされる
uv run jpr build-index                     # 索引構築。full 辞書で約 10 分 / 2.6GB
uv run jpr build-index --dict core --force # 小さい辞書で素早く作り直す

uv run jpr similar 乳首                     # 1 語 -> 音が近い語
uv run jpr phrase わたしのなまえは            # 1 フレーズ -> 語 + 助詞の連なり (空耳)
```

**Rust ツールチェインが必要。** 編集距離は `rust/` の拡張が計算するので
(`jpr.distance` が `jpr_distance` を import する)、無いと動かない。`uv sync` が
maturin 経由でビルドする — `[tool.uv.sources]` で `rust/` をローカル依存にしてある。
Rust 側を編集したら `uv sync --reinstall-package jpr-distance` で入れ直す。

**`sudachidict-full` は任意の依存** (`[project.optional-dependencies]` の `full`)。
CLI の既定 (`--dict full`) と full 索引の構築に要るので開発では入れるが、**実行時に
どちらを読むかは索引の `dict_type` が決める**ので (`PhoneticSearcher.extractor`)、
core の索引で動かすコンテナでは一度も読まれない。それでいて 344MB あり、索引
(core 148MB) より大きい死荷重になっていたので本体の依存から外した。

索引は `~/.cache/jpr/index` (`default_store_path()`)。`--index DIR` で切り替えられる。

`tests/test_acceptance.py` は実辞書の索引を要求し、未構築なら自動スキップされる
(`conftest.py` の `real_store` フィクスチャ)。それ以外のテストは `sample_store` の
小さな語彙 (18 語) で完結するので索引なしで走る。**受入テストがスキップされている状態を
「通った」と見なさないこと** — 検索品質の回帰はここでしか捕まらない。

`compare` と `pronounce` は索引を必要とせず、Sudachi 辞書だけで動く。

## アーキテクチャ

オフライン (索引構築) とオンライン (検索) の 2 経路があり、両者は `store.py` の
ファイル形式と `embedding.py` の `SPACES` 定義だけで繋がる。

検索エンジンには 3 つの窓がある。いずれも `PhoneticSearcher` を呼ぶだけで、
検索のロジックは持たない。

- `cli.py` — 人が端末から。プロセス起動ごとに Sudachi と索引を開く。
- `mcp.py` — LLM から (stdio)。
- `web.py` — 人がブラウザから。HTTP API + `static/` の素の HTML/JS。
  索引を 1 度だけ mmap して常駐するので CLI のコールドスタートを払わない。

検索の**経路は 2 つ**あり、入力の長さで使い分ける。

- **1 語 → 1 語** (`search.py`)。既定。音が近い語を索引から引く。
- **1 フレーズ → 語 + 助詞の連なり** (`phrase.py`)。空耳・替え歌。
  長い入力に音が近い**単一の語は辞書に存在しない**ので、前者では答えが返らない。
  入力をモーラ境界で区間に切り、区間ごとに語を当てて繋ぐ。

### 2 段検索 (`search.py`)

1. **候補生成** — `phonetic` 空間のベクトル行列と内積を取り、上位 K 件
   (既定 5000) を rerank に渡す。**ANN (HNSW) は使わない** — 下の項を参照。
2. **rerank** — weighted phonetic edit distance + 語尾・母音・モーラ数・一般性の
   重み付き和。精度をここで稼ぐ。

#### ANN をやめた理由 (`_top_candidates`)

**hnswlib の `load_index` はグラフをヒープに実体化する** (mmap のオプションが
無いことを確認済み)。core (111 万語) で匿名メモリ 652MB、内訳はグラフ 346MB と
**ベクトルの複製 306MB**。後者は `vectors-phonetic.npy` と同じデータの二重持ちで、
`M` を下げても消えない。実測:

| M | メモリ | knn_query | recall@5000 |
|---|---|---|---|
| 24 | 652MB | 16ms | 0.9860 |
| 12 | 481MB | 6ms | 0.9701 |
| 8 | 443MB | 6ms | 0.9316 |
| 4 | 408MB | 4ms | 0.7579 |

M=4 まで削っても 408MB が下限で、そこでは recall が 0.758 に崩れる。一方
**総当たり (mmap + GEMV) は匿名メモリ +4MB で 11〜16ms** (内積 8〜9ms +
`argpartition` 3〜7ms)、recall は 1.0。M=24 の 16ms と同等なので**速度を落とさず
メモリだけが消えた**。2GB の本番が OOM killed になった主因はこれだった。

かつては「総当たりは ANN より遅い (21〜30ms 対 15ms)」「大きく上げるなら ANN を
やめる判断のほうが筋が通る」と書いていた。前者は full (202 万語) での測定で、
core では逆転している。**語数が変われば測り直す。**

`candidates` (K) の意味も変わった。**内積は全行に対して取るので K を増やしても
候補生成は重くならない**。伸びるのは rerank だけで、「Top-K に入らない語を
rerank が拾えない」という取りこぼしも無くなった (ANN 時代は k=5000 で総当たりとの
top10 一致率が 0.73 しかなかった)。

embedding 単独に任せないのは「なぜ近いのか」が曖昧になるため。最終スコアの重みは
用途別プリセット (`pun` / `rhyme` / `mishearing`) で切り替える。

rerank は候補全件を配列演算で処理する。編集距離も `edit_distance_batch` で 1 度の DP に
まとめる (下の「音素距離」参照)。

編集距離だけは候補数に対して重いので、**候補が数万件を超えるときは順位に入り得ない候補に
距離を計算しない** (`_survivors`)。編集距離以外の成分は先に確定するので、そこに
`weights.phoneme` を足した値がその候補の取り得る最大スコアになる。上限の高い
`_PROBE_CANDIDATES` (32768) 件だけ先に本計算して打ち切り線を求め、線を超えられない候補を
落とす。**上位の順位とスコアは全件計算と一致する** — 近似ではない
(`tests/test_acceptance.py::test_edit_distance_pruning_does_not_change_ranking` が検証する)。

効くかどうかは `weights.phoneme` が決める。上限は距離が最良だった場合を仮定するので、
重みが大きいほど上限が緩く候補が落ちない。4〜6 モーラ (53 万候補) の実測:

| `weights.phoneme` | 0.20 | 0.30 | 0.40 | 0.50 | 0.60 | 0.65 |
|---|---|---|---|---|---|---|
| 生存率 | 1.8% | 7.2% | 23.7% | 62.7% | 96.9% | 99.9% |

生存率が高いと標本の距離計算がそのまま無駄な上乗せになるので (mishearing で 237ms →
348ms と悪化した)、**`_MAX_PHONEME_WEIGHT_FOR_PRUNING` (0.45) を超える重みでは枝刈りを
試みない**。通るのは `rhyme` (0.20) だけで、そこでは 23〜47% 速くなる。`pun` (0.50) は
境界付近で削減と標本コストが釣り合い、平均すると得がないので外してある。

比率で固定的に削る案 (上限上位 5% だけ計算) も測ったが、30 クエリで `pun` の top10 が
27/29 しか一致せず**真に上位の候補を落とした**。上限は `weights.phoneme` ぶん緩いので、
比率で切ると「まだ線を超え得る候補」が混ざる。**打ち切りは厳密にしか使えない。**

既定経路 (2000 件) では距離計算が 0.6ms しかないので判定コストのほうが高くなる。
`_PROBE_CANDIDATES` に届かない候補数では枝刈りを丸ごと省く。この経路では**スコアの
重み構成を距離計算の妥当性を気にせず変更できる**。

`_score_candidates` (スコアを配列で出す) と `_materialize` (上位だけを
`SearchResult` に起こす) を分けているのは、`store.entry()` が 1 件あたり Python
オブジェクトを作るので全候補に対して呼ぶと 2000 件で 40ms かかるため。必要なのは
同音異表記を畳んだ後に `limit` 件残る分だけ。**畳んだ後に件数が足りなければ選抜幅を
広げて起こし直す** — スコアは計算済みなのでやり直しは文字列の復号だけで済む。
この再試行を外すと、同音異表記の多いクエリで `limit` に届かなくなる
(`tests/test_acceptance.py::test_limit_is_filled_when_candidates_allow` が検証する)。

広げる上限は `_RETRY_LIMIT` (初回の 4 倍) で、**枝刈りが順位を保証する範囲と同じ値を
使わなければならない**。`needed` が大きいほど打ち切り線が下がって生存率が上がるので、
16 倍にすると 4〜6 モーラの検索が 326ms → 457ms と枝刈りなしより遅くなった。

`limit=None` は「上限なし」。スコア閾値 (`min_score`) で母集団を切ってその全件を
見る用途のための経路で、このときは選抜幅を広げる再試行をせず最初から全候補を起こす。

#### 候補生成の第 2 経路 — モーラ範囲の全走査

`min_mora` / `max_mora` を指定すると **Top-K を取らず、その範囲の行を全走査する**
(`_scan_candidates`)。既定の候補生成は `phonetic` 空間の Top-K なので、モーラ数の
違う語はそもそも候補に入らない — 「乳首」(3 モーラ) に対する「筑前煮」(5 モーラ) は
phonetic 空間で全体 154148 位、5 モーラ内に限っても 23823/301551 位で、`candidates` を
20000 に増やしても拾えなかった。**後段のフィルタ (`candidate_filter`) では解決しない**
のがここの要点で、Top-K に入った分しか見ないので同じ取りこぼしが起きる。だから
絞り込みではなく候補生成そのものを差し替える。

**内積を取る行数は既定経路と変わらない** (どちらも全行と内積を取る)。違うのは
rerank に渡す件数で、既定は上位 5000 件に絞るのに対しこちらは範囲の全行を渡す。
代償はそこで、暖まった状態で 5 モーラ単独 (30 万語) が 80〜140ms、4〜6 モーラ
(97 万語) で 125〜240ms、3〜8 モーラ (163 万語) で 200〜300ms — 既定経路
(15ms) の 10〜20 倍。幅はプリセットによるもので、`rhyme` は編集距離の枝刈りが
効くので下端に寄る (上の rerank の項を参照)。初回はページフォールトを含むので
さらに伸びる。

**この経路の計測は負荷の影響を強く受ける**。同じコードで load average 2 と 15 では
2 倍以上ずれるので、比較するときは `/proc/loadavg` を確認する。中央値ではなく最小値を
見るほうがノイズに強い。

**選抜した行だけを mmap から引くのではなく、全行と内積を取ってからマスクする**
(`_space_scores`)。直感に反するが 97 万行の選抜で 228ms → 16ms (14 倍) になる。
`vectors[rows]` の内訳は fancy indexing が 191ms・内積が 7ms で、コストは演算ではなく
飛び飛びに読んだ行を新しい配列へ実体化する部分にある。全行走査は連続読みなので対象が
2 倍あっても帯域で押し切れる。分岐点は候補が索引全体の 1〜2% で、既定経路 (2000 件 =
0.1%) は選抜のほうが速いため `_FULL_SCAN_RATIO` で切り替える。

かつては「全行と内積を取る案は 5 倍遅い」と書いていた。これは編集距離が全走査の 79% を
占めていて内積の差が埋もれていた頃の測定で、編集距離を Rust に移した (26 倍) 結果として
内積が最大の項目になり力関係が入れ替わった。**片方を速くすると別の場所が支配的になるので、
この種の判断は必ず測り直す。**

**分岐の判定に `np.unique` を使ってはいけない。** v5 で候補行をグループに写すように
したとき、「候補のグループ数」を数えるために `np.unique` を挟んだら 97 万候補 x 2 空間
でハッシュ化に 378ms かかり、**全走査が 285ms から 574ms へ 2 倍に伸びた**。行数は
グループ数の上界なので、分岐にはそれで足りる。重複を畳むのは選抜経路 (候補が少ない側)
に入ってからでよく、そこも**行が昇順ならグループ列も昇順**なので隣接比較で済む
(`np.unique` のソートが要らない)。同じ形は `phrase._match_chunk` にもある。

`_MAX_MORA_GAP` (ギャップ 3 で候補を落とす安全網) は **範囲指定時には適用しない**。
あれは Top-K 候補の粗さを補うためのもので、範囲を明示した検索では呼び出し側が意図を
持っている。適用したままだと 3 モーラのクエリから 7 モーラを要求したときに全件落ちる。

母集団を切り出す条件は `_selection_mask` に集めてある。store が列配列で持つ軸
(`mora_counts` / `costs` / `category_ids`) なら行を起こさずマスクを立てられるので、
モーラ以外の軸を足すときもここに `&=` で積む。**フィルタの抽象基盤は作っていない** —
軸ごとに「候補生成に使えるか」が違い (カテゴリは既定で常に効くので
`_apply_cheap_filters` 側にある)、統一の枠組みに押し込むとその差が消えるため。

### 分割合成 — 語 + 助詞の連なりを作る (`phrase.py`)

長い入力を「複数の語 + 助詞」に置き換える経路 (空耳・替え歌)。`jpr phrase` /
MCP の `compose_phrase` / Web の `/api/phrase` から入る。

    ワタシノナマエハ  ->  私 | の | 名前 | は

**なぜ別経路なのか**: 8 モーラの「ワタシノナマエハ」に音が近い単一の語は辞書に
存在しない。モーラ範囲の全走査 (上の項) でも解決しない — あれは「同じ長さの語」を
探す話で、そもそも該当する語が無い。人が空耳を作るときは入力を区間に切って別々の
語を当てているので、その操作を検索問題として解く。

3 段構成。区間の切り出し (モーラ境界のみ) → 区間ごとの照合 (rerank と同じ
weighted phonetic edit distance) → ビーム探索で連結。

**区間の母集団は「同じモーラ数の語」に限る。** 区間長 ±1 まで見ると 13 モーラの
入力 (46 区間) で延べ 1936 万行・3559ms かかるのに対し、同じモーラ数だけなら
延べ 303 万行・523ms。長さの伸縮は**区間の切り方そのものが担っている**ので
ここで払う必要がない (3 モーラの区間に 4 モーラの語を当てるのは、4 モーラの区間を
切るのとほぼ同じ)。

**助詞は内蔵の閉じた表で持つ** (`_PARTICLES`)。索引を引く案は成立しない —
`index.EXCLUDED_POS` が助詞・助動詞を落としているので 0 件しか返らない
(実測で の/を/に/が/は/で/と すべて 0 件)。さらに**索引には 1 モーラの語が
1 件も無い** (2 モーラ 38210 件に対し 1 モーラ 0 件) ので、1 モーラの区間を
埋められるのは内蔵表だけ。**利便のための表ではなく経路が成立するための必須部品。**

「は」「へ」「を」は**読みを 2 つ持たせる**。助詞としての発音は表記と違う
(ハ→[wa] 等) が、入力がどちらで書かれるかは決まらない。読みを ワ だけにしていた
とき、「ワタシノナマエハ」末尾の ハ で「は」(sim 0.717) が「さ」(サ、0.867) に
負けて**「私の名前は」が候補に出てこなかった**。

#### スコアは配分にする (加算にしない)

区間スコアは音韻類似度・一般性・表層の情報量の**重み付き和で 0〜1 に収める**
(`WEIGHT_PHONETIC` 0.72 / `WEIGHT_FAMILIARITY` 0.16 / `WEIGHT_INFORMATIVENESS`
0.08)。加算 (`similarity + 0.25 * familiarity`) にしていた頃はスコアが 1.25 まで
伸び、通常検索の 0〜1 と並べて読めなかった。

**情報量は一般性より軽くしなければならない。** 情報量は「漢字かどうか」しか
見ないので、重くすると稀な漢字語が既知の語に勝つ。0.12 対 0.16 で逆転していた頃は
「ワタシノナマエハ」が**「分か死の七異派」**になった。

**`cost <= 0` を一般性 1.0 と見てはいけない** (`UNKNOWN_COST_FAMILIARITY`)。
`index.familiarity_of` はコストの反転なので 0 が最良になるが、full 辞書の該当
51732 件 (既定カテゴリで 7299 件) は「炮り」「合ん」「合れ」「アがん」のような
活用の断片や稀な異表記で、実際には一般的でない。コストが振られていないことの
表れなので「最も一般的」ではなく**「わからない」**として扱う。`search` 側でこれが
問題にならないのは一般性が同点をほどく程度の重み (0.05〜0.15) しか持たないためで、
分割合成では一般性が候補の採否を決めるので効き方が違う。**同じ配列を使っていても
重みが変われば前提も変わる。**

数字・ラテン文字を含む表層は落とす (`_is_readable_surface`)。
`index.is_searchable_surface` は「かなか漢字が 1 文字でもあれば通す」ので
「2 士」「8 耐」が索引にあり、1 語だけ返す通常検索では目立たないが**列に混ざると
全体が読めなくなる** (実測で「コンニチハセカイ」が「婚 2 士 8 耐」になった)。
全角の英数字も同じ理由で落とす。

#### ビームの刈り方

刈る基準は**区間あたりの平均**。累積スコアで比べると区間数の多い経路が有利になり
(1 区間ごとにスコアが積み上がる)、同じ位置に届いた経路の優劣が「どれだけ細かく
刻んだか」で決まる。加えて語数そのものに減点を掛ける (`SEGMENT_PENALTY`) —
1 モーラずつ刻めばどんな入力でも音は合うが、それは空耳ではない。

刈るのは「これから伸ばす位置」で、直後の位置ではない。位置 *i* へは複数の区間長から
合流するので、*i* を処理する時点で初めて全部の流入が揃う。終端は伸ばす先が無く
ループが処理しないので別に刈る (刈らないと `limit` 件返すために数万本の経路を
`PhraseCandidate` に起こす)。

厳密な DP (位置ごとに 1 本) にはできない。**そこまでのスコアが最良の経路が続きで
詰まる**ことがあり、位置 5 までを 2 語で綺麗に埋めた経路より少し悪い 3 語の経路の
ほうが残りに合う語を持つ場合がある。

#### ラティス表示 — 候補を 1 枚の DAG に畳む

候補を並べると**同じ語が何度も出る**。実測で表示している区間の 65〜77% が重複し、
「名前」「は」「納衣」「に」「在り処」は上位 10 件の**全部**に現れた
(40 区間が 11 ノードに畳める)。`PhraseComposer.lattice` は同じ経路集合を
(区間, 表層) のノードと隣接関係で持ち、同じ語を 1 度しか出さない。

**ノード数を予算として与え、届くまでビーム幅を広げる** (`DEFAULT_NODE_BUDGET`)。
上位数件を畳んだだけでは構造が薄く (5 件で 8 ノード)、全候補では画面が埋まる。

**候補数 (`limit`) を増やしてもノードは増えない。** 経路の数はビーム幅で決まるので、
`limit=500` でも `beam_width=24` なら候補 24 件で飽和する。だから予算が動かすのは
ビーム幅のほう。広げるのは安く、8 モーラで beam 24 → 400 が 428ms → 293ms
(最初の 1 回が暖機を払っただけで実質横ばい) — 区間ごとの照合が支配的で、
ビームの刈り込みは幅にほとんど比例しない。

**予算とは別に「1 区間あたりのノード数」の上限が要る**
(`DEFAULT_MAX_NODES_PER_SPAN`)。予算は全体の数しか抑えないので、1 か所への集中を
防げない。「チクビ」(3 モーラ) は [0:3] に 10 個・[1:3] に 12 個が並び、
**24 行・高さ 1138px** の縦長になった。上限 6 を入れて 14 行・678px に収まった。

**削るときは経路を丸ごと落とす** (`_fit_lattice`)。ノードを個別に落とすと残りが
始端から終端まで繋がらなくなり「経路が読める図」でなくなる。スコアの高い経路から
順に採り、両方の制約に収まる範囲で組み直す。`tests/test_phrase.py` と
`test_acceptance.py` が「全ノードが完全な経路上にある」ことを検証する。

**図の横軸はモーラ位置ではない** (`layoutLattice`)。当初はモーラ位置に幅を比例
させていたが、1 モーラのノードが 78px しか取れず、隣の列が遠くに離れて**辺の
行き先が読めなかった**。開始位置で列を作り、列の幅は中身が要求する分だけ取り、
列の間隔を一定 (56px) にする。**上下の位置合わせは捨てる** — 位置を揃えることより
辺が短く追えることを取る。どのモーラを覆っているかはノード内の読みが示すので、
目盛りは要らない。実測で「わたしのなまえは」が 15 行 724px から 10 行 494px に
なり、辺の水平の隙間が必ず 56px 確保される (以前は図の幅いっぱいに伸びる辺があった)。

列の中の縦順は**前の列の繋がり先に寄せる**。経路数だけで並べると辺の縦の移動が
最大 414px (9 行ぶん) になった。直前のノードの行の平均を目標に並べ替えると 276px
まで縮む。それでも残るのは多くの経路が同じ語 (「名前」など) に収束する構造による
もので、並べ替えでは消せない。**縦に離れた辺は形だけでは追えない**ので、
ノードにカーソルを当てたときにその辺だけを立てて他を沈める (`.is-hot` /
`.is-cold` / `.is-near`)。

#### 実測 (2.02M 語, full 辞書)

モーラ数ごとの行の選抜 (`_pool`) は初回のみ 64〜330ms。**`PhoneticSearcher.composer`
が抱えて使い回す** — 区間ごとにマスクを立て直すと 13 モーラで 46 回払う。

| 入力 | 拍 | 区間数 | 時間 |
|---|---|---|---|
| ソラ | 2 | 3 | 4ms |
| チクビ | 3 | 6 | 18ms |
| ワタシノナマエハ | 8 | 26 | 186ms |
| スモモモモモモモモノウチ | 12 | 42 | 171ms |
| アルミカンノウエニアルミカン | 14 | 50 | 267ms |

繰り返しの多い入力 (スモモ…) が 14 モーラより速いのは、同じ音素列の区間を
1 度しか照合しないため。

### 5 つの埋め込み空間 (`embedding.py`)

`phonetic` / `consonant` / `vowel` / `coda` / `rhythm`。用途で「近い」の定義が変わるので
単一ベクトルに混ぜない。ニューラルな学習は行わず、音声学的素性から決定的に構成する。

- `phonetic`, `consonant`, `vowel`, `coda` は L2 正規化済み → 内積 = コサイン類似度。
- `rhythm` だけは正規化しない (モーラ数の絶対量が意味を持つ)。比較はユークリッド距離。
  `compare_pronunciations` がこの空間を特別扱いしているのはそのため。
- 候補生成に使うのは `CANDIDATE_SPACES` = `("phonetic",)` だけ。rerank が足すのは
  `coda` と `vowel`。**索引に保存するのはこの 3 つだけ** (`store.INDEXED_SPACES`)。
- **`consonant` と `rhythm` は索引に持たない。** 引くのは `compare` だけで、あちらは
  索引を引かず入力 2 語をその場で `embed()` するため。5 空間ぶん保存していた頃は
  full で 107MB・core で 58MB が読まれないまま置かれていた。
- ベクトル行列は mmap で読むので、**索引に載せる空間を増やしてもディスクだけが増える**
  (匿名メモリは増えない)。ただし読まない空間を載せる理由はない。

位置情報は等間隔ビンのマルチスケールプーリング (`POSITION_SCALES = (1, 2)`) で保つ。
**ここを細かくしてはいけない** — 4 ビンにすると語頭 1 音素の差でビンが丸ごと外れ、
「チクビ」と「テクビ」の類似度が 0.68 まで落ちて 候補の上位にすら入らなくなった。
細かい位置合わせは rerank 段の編集距離が担うので、候補生成側は粗くてよい。

### 音素距離 (`distance.py`)

音素を記号ではなく素性ベクトル (調音位置・方法・有声性・口蓋化 / 母音の高さ・前後・円唇)
として扱い、素性差から距離を導く。**素性は IPA の記述に合わせる** — 表記が ASCII の
ヘボン式寄りなのは `phonology.py` がかなから機械的に写せるからで、距離の根拠は記号では
なく素性側にある。だから「ローマ字で同じ字だから」ではなく「その音が実際にどう調音される
か」で素性を決める。`f` (フ・ファ行) を [ɸ] として `bilabial` に置いてあるのがその例で、
綴りに引かれて `labiodental` にすると alveolar と隣接して f-s が 0.067 まで落ち、
「フトン」の最近傍が「ストン」・「フウフ」が「スウフ」になる。`v` (ヴ) は借用語の [v] で
こちらは実際に唇歯音なので分けてある。

#### IPA 表記 (表示専用)

音素記号 -> IPA の対応表 `_IPA` を**素性表と同じファイルに置く**。距離計算には一切
使わないが、別ファイルに移すと素性を直したときに表記だけが古いまま残る (`f` を [ɸ] に
直したときに IPA 表が [f] のままになる類)。`tests/test_distance.py::
test_every_phoneme_has_an_ipa_entry` が表のキーで全音素を覆っていることを見る —
`phoneme_ipa` は未知の記号をそのまま返すので、漏れは例外にならず**表示だけが黙って
ヘボン式のまま残る**。

粒度は音素との 1 対 1 に留める。環境による異音 (ン の [m][n][ŋ]、母音の無声化) は
写さない — 距離側も異音を区別していないので、表示だけ細かくすると「近い理由」の説明と
食い違う。

**促音だけは表を引くだけでは書けない。** 促音は後続子音の重複として実現するので
(キッテ = [kitte])、音素 1 つに対応する記号を持たない。`ipa_transcription` が音素列
全体を見て重複に書き換え、単独記号の [ʔ] は後続子音が無いとき (語末の「あっ」) だけ
残す。破擦音の重複は先頭 1 文字だけを重ねる ([t͡ɕt͡ɕ] にはしない)。**Web の音素チップは
後続を見られないので促音の IPA を出さない** (`_NO_STANDALONE_IPA`) — チップに [ʔ] を
出すと語中でも声門閉鎖に見え、連続表記と食い違う。促音の音価は連続表記のほうで読ませる。

表示は 3 経路すべてに出る。CLI (`similar` / `compare` / `pronounce`) は音素列を `//`、
IPA を `[]` に入れる (角括弧が音声表記の慣用なので、既存の `[音素列]` を `//` に移した)。
MCP と Web の API は `ipa` フィールドを足す。Web は `/api/phonemes` で音素ごとの IPA も
配り、チップの併記は JS が写す — 色と同じ理由で、**UI 側に固定表を持つと素性表を変えた
ときに黙ってずれる**。ただし `g` (ASCII) と `ɡ` (U+0261) は別のコードポイントだが画面上
ほぼ同形なので、チップの併記からは外してある (`_LOOKALIKE`)。

Web の表示は 2 層に分かれ、**トグルで切り替わるのは音素ごとの併記だけ**。

- **語全体の連続表記は常に出す** — 結果カードの読みの隣、クエリの readout、読みビュー。
  発音そのものを読む表記であって音素記号の注釈ではないので、トグルの管轄外に置く。
  カードでは読みと同じ行に並べ、収まらない長さでは flex で折り返す。**独立行に
  するより高さが得**: 3 モーラで全カードが 139px に揃うのに対し、独立行は 165px
  (+19%) になって 1 画面の件数が減る。長い語 (6 モーラ) では独立行のほうが高さの
  種類が 3 → 2 に減るが揃いはしない (音素チップの折り返しが残る) ので、多数派の
  短い語を取る。
- **音素チップ 1 つずつの併記はトグル (既定 off)**。**on では枠幅を広げる**
  (`SLOT_PX` 250 → `SLOT_PX_IPA` 340) — 3 モーラのチップ列が 179px から 268px に
  広がり、250px の枠では行の内寸 253px に 15px 足りずに折り返す。折り返すと行の高さが
  不揃いになり、列をまたいだ順位の読み比べができなくなる。なお `SLOT_PX` の
  「折り返さない」という前提は IPA 抜きでも 4 モーラ程度までしか成り立っておらず、
  5〜6 モーラ (17〜18 音素) は 636px を要求して既に折り返している。

アライメントパネルに置くのは**クエリ側の連続表記だけ**。候補側は同じカードの読みの隣に
常時出ているので、両方並べると 1 枚のカードに同じ IPA が 2 回出る。

同じ距離関数に 2 つの実装がある:

- `weighted_edit_distance(tuple[str], tuple[str])` — 記号ベースの Python 実装。可読性優先。
  `compare` と `align_phonemes` が使う。**素性表を変えたときの基準はこちら。**
- **Rust 実装** (`rust/src/lib.rs`) — rerank の本線。Python 側の入口は 2 つある。
  - `edit_distance_csr(...)` — 索引の CSR (連結した音素 ID + 境界) をそのまま渡す。
    **渡す添字は行ではなくグループ** (v5、`store.phoneme_csr` の項を参照)。
    **検索が通るのはこの経路。** パディング行列を Python 側で組む処理が消える。
  - `edit_distance_batch(query, (C, L) 行列, 長さ配列)` — パディング行列を渡す経路。
    索引を用意せず任意の音素列を投げられるので、記号版との一致を検証する足場に使う。
    パディングは `PAD_ID`。

**2 者は同じ値を返さなければならない** (`tests/test_distance.py` が対応を検証する。
手で選んだ対に加えてランダムな長短混在の行列 200 セットで突き合わせる)。距離の重みや
素性表を変えるときは Rust 側にも効くことを確認する — コスト表は Python が素性表から
構成して渡すので、**重みを変えるだけなら Rust を触る必要はない**。DP のロジック自体を
変えるときだけ両方を直す。とくにバッチ版はパディングの扱いを間違えると長さの違う候補
だけが静かに狂うので、長短の混ざった行列で検証する。`_worst_substitution_cost()` は
素性表から実測するので、重みを変えても類似度の正規化は自動追従する。

素性表の変更が索引に及ぶかは軸で違う。`embedding.py` は調音位置と方法を **one-hot** で
持つので、`_PLACE` の序数を変えるだけなら索引はそのまま使える (rerank の距離しか動かない)。
一方 **母音の `height`/`backness`/`rounded` は連続値のまま埋め込みに入る**ので、/u/ の
前後を触ると索引を作り直すことになる。`_PLACE` / `_MANNER_DISTANCE` のキーを増減させた
ときも one-hot の次元が変わるので同じ。

音声学的に粗く見えても**順位に効いていない箇所がある**ので、直す前に測る。実測で外した例:

- **撥音の異音を無視している点** — ン は後続で [m][n][ŋ][ɴ] と交替するのに `N` と鼻子音の
  距離は一律 0.35 で、`N`-`g` は cross-class の 1.0。一見すると「サンガク/サガク」を
  取り逃がすが、**実際には DP がこの経路を通らない**。`N` の挿入削除が
  `_SPECIAL_INDEL_COST` (0.45) なので、置換 (1.0) より削除のほうが安く 0.45 で繋がる。
  異音表を足しても順位は動かない。
- **/h/ を glottal 固定している点** — ハヘホ [h] は正しいが ヒ は [ç]、フ は [ɸ]。ただし
  `hy` は既に `palatal` にしてあり、`h`-`k` も 0.327 で velar と隣接しているので不当に
  遠くはない。残っていた実害は `f` のほうだけだった。

DP の 2 行は**参照を入れ替える** (`std::mem::swap`)。行を `copy_from_slice` で書き戻すと
クエリ音素ごとに m+1 要素の書き込みが増え、DP 本体と同じ量になる。

NumPy のバッチ実装は持っていた (候補方向をベクトル化して DP の 1 行を全候補ぶん進める形)
が、53 万候補で 1097ms 対 41ms と 26 倍差がつき、2 実装を揃えて維持する意味が無くなった
ので削除した。NumPy では (クエリ長 x 候補数 x 音素長) の置換コストテンソルを実体化して
しまう (float32 でも 153MB) のに対し、Rust は 1 候補ずつ DP を回して候補方向を rayon で
並列化できる。作業配列は最大 25 要素なので L1 に収まり、帯域を使わない。構造的な差なので
NumPy 側をどう書いても追いつけない。

### 索引形式 (`store.py`)

`FORMAT_VERSION` を上げる変更 (エントリの持ち方・空間の追加削除) をしたら、既存の索引は
`jpr build-index --force` で作り直す必要がある。`PhoneticStore.__init__` がバージョン
不一致を検出してエラーにする。

pickle を使わない理由は 2 つ — ロードに 12〜18 秒かかり MCP サーバの起動コストとして
重すぎたこと、任意コード実行を招く形式を配布物に使いたくないこと。NumPy の mmap なら
実質 0 秒。文字列は固定幅 unicode 配列 (`<U27`) を避け、UTF-8 バイト列 + 境界インデックス
(CSR 風) で持つ (`<U27` は最長要素の幅で全行を埋め、202 万語の表層だけで 218MB 消費した)。

置くファイルは `meta.json` と `<配列名>.npy` (語彙メタデータ) と
`vectors-<空間>.npy` (3 空間ぶん)。**まとめた npz にはしない** — mmap できなくなる
(下の項を参照)。

200 万件を起動時に materialize しない。`entry(row)` は必要になった行だけ Python
オブジェクトに起こす。フィルタは `category_ids` / `mora_counts` / `costs` の配列を
直接引いてベクトル化する (`_apply_cheap_filters`)。

#### ベクトルは int8 に量子化する (`_quantize`)

**v4 の変更。** float32 のままだと full で 1.47GB あり、ディスクとページキャッシュの
両方を圧迫していた。int8 に落とすと 368MB になる (1/4)。

full (202 万語) の実測:

| 空間 | float32 | int8 |
|---|---|---|
| phonetic | 583MB | 146MB |
| consonant | 364MB | 91MB |
| coda | 388MB | 97MB |
| vowel | 73MB | 18MB |
| rhythm | 65MB | 16MB |
| **計** | **1473MB** | **368MB** |

`entries.npz` も境界を int32 にして 164MB -> 140MB。**索引全体では 1.64GB -> 508MB。**

サイズだけでなく**速度にも効く** — 候補生成は帯域律速なので、読むバイト数が 1/4 に
なったぶん内積が速くなる (上の「候補生成の内積」の項)。

#### ベクトルは音素列グループごとに 1 行だけ持つ

**v5 の変更。** 埋め込みも編集距離も**音素列だけの関数**なので (`embedding.embed` は
`Pronunciation` しか見ない)、同音異表記 (「価格」「架格」「カカク」) はまったく同じ
ベクトルと同じ距離を持つ。行ごとに持つと full の 202 万行のうち 146 万ぶんしか情報が
無いのに全部を保存し、内積も重複して計算することになる。

全 5 空間・全 202 万行がグループ先頭のベクトルと**完全一致する**ことを実測で確かめて
ある (`tests/test_store.py::test_vectors_are_stored_once_per_phoneme_group`)。

| | 行 | グループ | 比 |
|---|---|---|---|
| full | 2023395 | 1459159 | 0.721 |
| core | 1114517 | 637353 | 0.572 |

**core のほうが重複が多い。** 語数が少ないぶん異表記の比率が上がるため。

行からグループへは `group_ids`、グループから行へは `group_starts` で写す。候補生成は
グループで Top-K を取ってから行へ展開する (`search._expand_groups`)。これは
`candidates` の意味も変える — **上位 8000 件が「異なる音素列 8000 個」になる**ので、
同じ音の異表記で候補が埋まらなくなった。

音素列の CSR も同じ理由でグループ単位にした (21.7MB -> 17.5MB)。rerank が同音異表記の
距離を代表 1 件に畳んでいた処理 (`_group_representatives`) は、**索引の側が畳まれた
ことで入力がそのままグループ列になった**。

#### 索引に持つのは検索が読む 3 空間だけ (`INDEXED_SPACES`)

**v5 の変更。** `embedding.SPACES` は 5 空間あるが、索引から読まれるのは
`phonetic` (候補生成) と `coda` / `vowel` (rerank) の 3 つだけ。`consonant` と
`rhythm` を引く経路は `compare` (`compare_pronunciations`) しかなく、**あちらは索引を
引かず入力 2 語をその場で `embed()` する**ので行列が要らない。full で 91MB + 16MB、
core で 58MB が死蔵されていた。

**rerank に空間を足すときはここに加える** (索引の再構築が要る)。`embed()` は
`spaces` 引数で作る空間を選べるので、構築側も 200 万語ぶんの捨てるベクトルを
計算しなくなった。

#### 語彙メタデータは npz にまとめない

**v5 の変更。** npz は zip なので **mmap できず**、`np.load` が全配列をヒープに展開
する。full の実測で 133MB がまるごと匿名メモリ (回収不可) に載り、ベクトル行列を
mmap にした意味を相殺していた。1 配列 1 ファイル (`surface_blob.npy` 等) に分けると
同じ 133MB が**ページキャッシュ (回収可能) に移る**。

**圧縮していないのでディスク上の大きさは変わらない** (実測 133.5MB で同じ)。得られる
のは匿名メモリだけ。full の実測:

| | v4 (npz) | v5 (個別 .npy) |
|---|---|---|
| store を開いた直後の RssAnon | 149MB | **15MB** |
| 全走査まで通したピーク RssAnon | 277MB | **158MB** |

**これは OOM の余裕に直接効く** — 2GB 制限のコンテナで回収不可な領域が半分になる。

**スケールは空間ごとに 1 つ。** 行ごとに持つ案も測ったが、`phonetic` は L2 正規化済みで
行の最大値が 0.24〜0.49 に収まるため再構成誤差が変わらなかった (どちらも 0.0026)。
スケール配列を別に持つ複雑さに見合わない。`rhythm` だけは正規化しない (ノルム最大 2.24)
が、空間ごとに最大値を取る形なら自動的に追従する。

**誤差は順位を揺らす。** 内積の誤差は最大 0.012 (全 5 空間の実測) で、Top-K 境界の
スコア差 0.0002 より大きい。候補生成の recall で吸収する — `DEFAULT_CANDIDATES` を
8000 に広げると float32 基準で 0.997 まで戻る (上の項を参照)。**rerank に渡った後の
スコアにも誤差は乗る**が、最終順位への影響は測って確かめてある (下記)。

#### 境界インデックスは int32

表層・読み・音素の CSR 境界は int32。full でも blob は最大 3900 万バイトしかないので
int64 の幅が要らない (3 本で 24MB の差)。**2^31 を超える blob は表現できない**ので、
`_check_blob_fits` が構築時に検証して落とす — 溢れると境界が負に回り、索引全体が
静かに誤った文字列を返す。

`edit_distance_csr` の Rust 側も int32 を受けるようにした。int64 を要求したままだと
呼び出しごとに 16MB の変換コピーが走る。

### 辞書パース (`dictionary.py`)

SudachiPy には語彙列挙 API が無く (`lookup` は完全一致のみ)、Java 版の `dump` も
提供されないため `system.dic` のバイナリを直接パースしている。`_validate_offset_table()`
がオフセットテーブルの整合性を起動時に検証するので、SudachiDict のフォーマットが
変わればそこで失敗する。**このガードを外さないこと** — 読み違いが黙って通ると
索引全体が静かに壊れる。

### カテゴリ (`index.py`)

SudachiDict full は地名 49 万・人名 24 万を含み索引の 7 割を占める。人名の異表記は
音韻的に密集するので、`DEFAULT_CATEGORIES` は `person` / `place` を除く。

`familiarity` は Sudachi の連接コストを反転した「知られた語らしさ」の弱い指標。厳密な
頻度ではないので、順位の同点をほどく程度の重みしか掛けない (`PRESETS` の `familiarity`)。

## 実装上の注意

- **同音異表記の代表選び** (`_representative_rank`): SudachiDict では読みをそのまま
  書いたカタカナ見出し (「カカク」cost 3269) が漢字表記 (「価格」cost 5496) より低コスト
  なことがある。コストだけで選ぶと「科学」の近傍が「カカク」になり語として何を指すのか
  読み取れないので、見出しの情報量 (`_surface_informativeness`) をスコアより先に見る。

  **最後に表層そのものを見るのは決定性のため。** 情報量・スコア・一般性がすべて同じ候補が
  実在する (「仕組」と「仕組み」、「焦立とう」と「苛だとう」)。`familiarity` は
  `COST_FAMILIAR` (5000) 以下のコストが全部 1.0 に飽和するので、cost 4500 と 5000 を
  分けられない。表層を足さないと代表が候補の到着順で決まり、候補生成の経路や編集距離の
  打ち切りを変えるだけで**同じクエリが違う表記を返す**。符号順という決め方自体に意味は
  なく、再現性だけを与えている。
- **`DEFAULT_CANDIDATES = 8000`**: 202 万語ではコサイン 0.91 以上の語が 400 件を超えるため、
  400 に絞ると「乳首」に対する「手首」のような明らかな近傍が漏れる。編集距離をバッチ化した
  今は 400 で 7.5ms / 5000 で 27.5ms と差が 20ms しかないので、品質を取る判断が明確に有利。
  (バッチ化前は 5000 で平均 440ms・最悪 1.4 秒かかっていた。)

  **5000 -> 8000 は量子化の誤差を吸収するため。** int8 の内積は誤差が最大 0.012 あり、
  Top-K 境界のスコア差 (0.0002 程度) より大きいので候補生成の順位が入れ替わる。
  float32 の Top-5000 を正解としたときの recall は k=5000 で 0.919・k=6000 で 0.977・
  **k=8000 で 0.997**。候補生成は k に依らないので、広げて取りこぼしを消すほうが得。

  **k は「異なる音素列の個数」になった** (v5)。Top-K をグループで取るので、
  8000 件が同じ音の異表記で埋まらない。行に展開したあとの候補数は 8000 より
  多くなる (full の平均で 1 グループ 1.39 行)。

  **候補生成そのものは k に依らない。** 内積は全行に対して取るので、k は
  「rerank に何件見せるか」だけを決める (`_top_candidates` の項を参照)。ANN を
  使っていた頃は k を上げると探索が線形以上に遅くなり (k=20000 で 62ms)、かつ
  Top-K に入らない語を rerank が拾えないという取りこぼしがあった (総当たりを正解と
  した top10 一致率は k=400 で 0.43・k=5000 で 0.73・k=20000 で 0.89)。いまは
  内積の順位が正確なので、k を上げて増えるのは rerank のコストだけになった。
- **候補生成の内積と Top-K は Rust + int8 が持つ** (`store.top_groups` /
  `rust/src/gemv.rs`)。full 202 万語 x 72 次元、**プロセスを分けて 20 回の最小値**
  (下の測定上の注意を参照):

  | | 内積 | Top-K |
  |---|---|---|
  | NumPy float32 (BLAS + `argpartition`) | 15.4〜26.0ms | 9.0〜15.5ms |
  | Rust int8 | 16.8〜28.8ms | 2.6〜10.0ms |

  **内積は BLAS と互角。int8 にしても速くならない。** 読むバイト数は 1/4 になるが、
  202 万行の走査はどちらも DRAM 帯域で決まり、int8 の積和は BLAS の SIMD された
  f32 積和より 1 要素あたりのスループットが出ないので相殺される。「Rust の内積は
  BLAS に負ける」という以前からの結論は**量子化しても変わらない**。

  **量子化の利点はサイズであって速度ではない** (索引 1.64GB -> 508MB)。速度で
  実際に得しているのは Top-K のほうだけ。

  **行数を削るほうは素直に効く** (v5 のグループ化)。内積 + Top-K を別プロセスで
  測ると 202 万行 10.3ms -> 146 万行 7.5ms で、削減率 28% とほぼ一致する。
  ただし**検索全体では埋もれる** — 既定経路は rerank と結果の実体化が支配的なので、
  エンドツーエンドの実測はベースラインと同等 (11〜13ms) だった。**この経路を
  速くしても検索は速くならない**ので、狙うならほかの項目を測ってからにする。

  **測定上の注意 — f32 と int8 を同じプロセスで交互に測ってはいけない。**
  最初にそうやって測ったとき int8 が 6.2ms・f32 が 16.3ms となり「2.6 倍速い」と
  読んでしまった。f32 の行列は 582MB で L3 に載らず、交互実行では f32 が毎回
  キャッシュを流すので**次に走る int8 (145MB) だけが有利になる**。プロセスを
  分けて独立に測ると両者は並ぶ。**共有資源を奪い合う 2 案の比較は、必ず別々の
  プロセスで測る。**

  **NumPy 側で量子化しても速くならない。** int8 の GEMV 経路が無いので
  `astype(np.int32)` を挟むことになり、10ms かかるうえ 202 万行ぶんの中間配列
  (582MB) を実体化する。float16 に至っては 76ms と f32 より遅い。

  **内側のループは素朴に書く。** 4 アキュムレータへの手動アンロールを試したら
  6.2ms -> 19.2ms と 3 倍遅くなった。素朴な形なら LLVM が自動ベクトル化するが、
  手で展開すると崩れる。`-C target-cpu=native` にも頼らない (generic ビルドで
  既に 6.2ms 出るので、配布物の互換性を捨てる理由がない)。

  Top-K は `argpartition` の 7.8 倍速い。全体を 1 度に `select_nth_unstable` へ
  渡すと単一スレッドになるので、チャンクごとに局所 Top-K を取ってからマージする。
  **同点は行番号まで見て全順序にする** — スコアだけで比べるとスレッドの合流順で
  並びが変わり、代表選び (`_representative_rank`) が非決定的になる。
- **速度のテストは最小値で見る** (`test_acceptance.py::test_search_is_fast`)。
  候補生成が索引全体の内積になったので、計測が負荷とページキャッシュの状態に
  強く引きずられる。実測で **load average 2 と 38 で同じコードが 40ms と 320ms に
  ぶれた**。平均を取ると重い 1 回に持っていかれるので「最も条件が良かった 1 回」を
  基準にし、閾値は桁違いの退行 (素朴な実装の 12 秒) だけを捕まえる 500ms に置く。
  **緩めた閾値が意味を持つことは確かめてある** — 候補生成に 1 秒の遅延を注入すると
  load 69 でも落ちる。
- **`ReadingExtractor` のキャッシュはインスタンス単位**: メソッドに `lru_cache` を付けると
  クラス単位のキャッシュが `self` を握り続けインスタンスが解放されなくなる。
- **Sudachi の `Tokenizer` はスレッドセーフではない**: 解析用バッファを内部に持つので
  複数スレッドから `tokenize()` に入ると Rust 側が `RuntimeError: Already borrowed` を
  投げる。**web.py の非 async なエンドポイントは starlette のスレッドプールで動く**ため
  同時リクエストがそのまま衝突し、本番の `/api/similar` が 500 を返した。
  `ReadingExtractor._lock` で直列化している。`tokenize` は 1 語あたり 1ms 未満で
  キャッシュも効くので、スレッドごとに tokenizer を持つ (Sudachi 辞書を重複して抱える)
  より安い。**`Morpheme` は解析器のバッファを参照するので、読み出しまでロック内で
  終える** — 生成子のままロックを出すと同じ問題が残る。

  **単発のテストでは踏まない。** キャッシュが埋まると `tokenize` に入らないので、
  回帰テスト (`test_reading.py::test_concurrent_access_does_not_break_the_tokenizer`) は
  毎回キャッシュを捨てて 8 スレッドで叩く。HTTP 経由も
  `test_web.py::test_concurrent_requests_do_not_return_errors` で押さえてある。
  内積と Rust の編集距離は 8 スレッド並行で問題が出ないことを確認済み。
- **Sudachi のロードは重い**: `PhoneticSearcher.extractor` は初回参照まで遅延させている。
  索引 (mmap) も MCP サーバ・Web サーバでは最初のリクエストまで開かない。実測で
  初回 12.5 秒、2 回目以降は 34ms〜1 秒。

### Web フロント (`web.py` + `static/`)

- **ビルド工程を持たない**: 素の HTML/CSS/JS を `static/` に置き、FastAPI が配る。
  Node の依存とビルド手順を増やすほどの UI ではない。
- **音素の色は素性表から決める** (`/api/phonemes`): 子音は調音位置、母音は舌の位置で
  色相が決まる。UI 側に色の固定表を持つと `distance.py` の素性表を変えたときに黙って
  ずれるので、素性そのものを配って JS に写させる。
- **`align_phonemes()` は表示専用ではない**: `weighted_edit_distance` と同じコスト定義で
  DP を回して経路を復元する。**対ごとのコストの総和は編集距離に一致しなければならない**
  (`tests/test_distance.py::test_alignment_costs_sum_to_edit_distance` が検証する)。
  距離の重みを変えたらここも追従する。
- **スコア棒は結果内で正規化する**: 音韻スコアは上位が 0.90〜0.98 に密集するので、
  0〜1 をそのまま幅に写すと全部が満杯に見えて順位差が読めない。棒は相対差、
  数値が絶対値という二重表現にしている。
- **分割合成ビューはモーラ列の枠に載せない**: `.mora-columns` は「モーラ数ごとに列を
  立てて順位を読み比べる」ための配り方だが、合成候補はどれも入力と同じモーラ数なので
  列に分ける軸がない。1 件が区間の対応帯を持って縦に伸びるため 1 列で積む。
- **区間の幅はモーラ数に比例させる** (`flex-grow`): 等幅にすると 1 モーラの助詞と
  4 モーラの語が同じ幅になり、「入力のどこを覆っているか」が読めなくなる。区間は
  入力を隙間なく覆うので、帯全体の幅が入力の長さに対応する。助詞は沈めた背景で出す
  (語と同じ強さで並べるとどこが意味を持つ語なのかが読み取れない)。
- **分割合成の既定値も API から配る** (`/api/info` の `phrase`): 音素の色と同じ理由で、
  フロントに固定表を持つと `phrase.py` の定数を変えたときに初期値が黙ってずれる。
- **ラティス図はノードを div・辺を SVG で描く**: 辺を DOM 要素にすると曲線が引けず、
  ノードを SVG に入れるとテキストの折り返しが効かない。両方の都合を取っている。
  辺のベジエは**制御点を両端に寄せる** (`dx = (x2-x1) * 0.55`) — ノードの縁から
  横向きに出るので、1 つのノードに束になった辺の行き先が見分けやすい。中点に
  制御点を置くと縁で辺が重なる。
- **絞り込みで外れたノードは消さずに沈める** (`.is-dim`): 消すと図の形が変わって
  どこを絞ったのかが読めなくなる。経路数はノードの地の濃さと辺の太さに写す
  (数値は title に持たせる。スコア棒と同じ二重表現)。
- **`.seg` はセレクタを親で限定する**: 検索タブのプリセットと分割合成の見せ方の
  切り替えが同じクラスを使うので、`document.querySelectorAll(".seg")` で引くと
  片方を押したときにもう片方の状態を壊す (`activePreset` が undefined になった)。
  グループの id (`preset-group` / `phrase-view-group`) の中だけを見る。

## デプロイ (`Dockerfile` / `fly.toml`)

**Serverless には載らない。** 索引が core で 274MB・full で 508MB あり、Vercel /
Lambda の関数サイズ上限 (250MB) を超える。圧縮しても解凍後サイズで判定されるので
回避できない。加えて Rust 拡張のビルド (`jpr_distance` が無いと import に失敗する) と
mmap 常駐の前提があり、**この 3 つはいずれもコンテナを要求する**。だから
コンテナ系 PaaS (Fly.io) を選んでいる。

**v5 で core が 148MB になり、索引は上限 250MB を初めて下回った** (量子化前 855MB ->
int8 で 274MB -> グループ化と 3 空間化で 148MB)。**サイズはもう Serverless を
阻んでいない。** ただし残り 2 つの理由 — Rust 拡張のビルドと mmap 常駐 — は消えて
いないので、載せるならその 2 つを解く必要がある。

**索引はイメージに焼き込む** (Volume に置かない)。Volume だと「初回に索引を用意する」
手順がデプロイと別に必要になり、`fly deploy` 一発で動く状態にならない。ビルド時に
`build-index` を走らせる代償はイメージ 2.77GB・ビルド 8 分だが、Fly の上限 8GB には
収まる。

**辞書は core。** 索引の実測 (v5):

| | v4 | v5 | 内訳 (v5) |
|---|---|---|---|
| full (202 万語) | 508MB | **322MB** | ベクトル 188MB + メタ 133MB |
| core (111 万語) | 274MB | **148MB** | ベクトル 82MB + メタ 66MB |

v4 -> v5 の削減はベクトル側だけで、**グループ化 (-28%) と 3 空間化 (-29%) の積**。
メタデータは npz を個別 .npy に分けただけなので大きさが変わらない (mmap のために
分けたので、効くのは匿名メモリのほう)。

core に落としても検索品質は保たれることを実測した —
「乳首」→ 手首、「わたしのなまえは」→ 私の名前は が同じ順位で出て、検索 16〜22ms・
`compose` 281ms・索引のロード 0.4 秒。full の 202 万語が効くのは稀な語の再現性なので、
公開デモでは core で足りる。**内積が語数に比例するので、full にすると検索が伸びる。**

**`hnsw-*.bin` を持ち込まないこと。** ANN をやめたので `build-index` は
グラフを生成しないが (`_top_candidates` の項を参照)、古い索引ディレクトリには
`hnsw-phonetic.bin` (558MB) や `hnsw-coda.bin` (451MB)、さらに古い
`hnsw-consonant.bin` (915MB) が残っている。**読まないので動作には影響しないが、
そのままコピーするとイメージが無駄に膨らむ**。`--force` で作り直せば消える。

**`LICENSE` を COPY し忘れると uv のビルドが落ちる。** `pyproject.toml` の
`project.license-files` が参照するので、メタデータ検証の段階で
`glob 'LICENSE' did not match any files` になる。

**`--host 0.0.0.0` が必須。** `serve-web` の既定は `127.0.0.1` なのでコンテナ外に
届かない。

#### メモリ — `RssAnon` と `RssFile` を分けて見る

2GB で **OOM killed** になった (`anon-rss 1.9GB` で SIGKILL、60 並行に対して
502 が 15 件)。500 (`Already borrowed`) を直した後に別の理由で落ちていたので、
**502 と 500 を区別してログを読む** (502 はプロキシがアプリに届かない =
プロセスが死んでいる、500 はアプリ内の例外)。

**RSS だけを見ると原因を取り違える。** `/proc/<pid>/status` の `RssAnon`
(匿名・回収不可) と `RssFile` (mmap・回収可能) に分けると、全走査の実測は:

| | RssAnon | RssFile |
|---|---|---|
| 暖機後 | 758MB | 75MB |
| 全走査後 | **758MB (増えない)** | 491MB |

つまり当初「全走査が一時配列で +279MB 使う」と読んだ増加は**大半が mmap の
ページキャッシュ**で、OOM の原因ではなかった (母集団ぶんの配列は内積 2.3MB・
rows 4.6MB・mora/costs 4.6MB で合計 10MB 未満)。**真の原因は HNSW の 620MB**
(上の `_top_candidates` の項)。ANN をやめて full (202 万語) でも常駐 260MB に
なった。

**v5 で匿名メモリがさらに半分になった。** 語彙メタデータを npz から個別の .npy に
分けて mmap で開くようにしたため (上の「語彙メタデータは npz にまとめない」の項)。
full を同じ手順で通した実測:

| | v4 | v5 |
|---|---|---|
| store を開いた直後 | 149MB | **15MB** |
| 検索 20 回 | 262MB | 136MB |
| 全走査まで通したピーク | 277MB | **158MB** |

npz は zip なので mmap できず、`np.load` が 133MB をヒープに展開していた。
**mmap にしてもディスク上の大きさは変わらない** — 動くのは匿名メモリだけで、
それがちょうど OOM の効く軸だった。

ただし全走査は**並行度に比例して匿名メモリを積む**ので門は残す。full の実測
ピークは 2 並行 +190MB・5 並行 +357MB・10 並行 +613MB。`web.py` の
`MAX_CONCURRENT_SCANS` (2) なら 451MB で収まる。**門を通すのは全走査経路だけ** —
既定経路は 10 並行 x30 回でも +60MB 程度なので、軽いリクエストを待たせる理由が
ない。取れないときは `SCAN_QUEUE_TIMEOUT` (30 秒) で諦めて 503 を返す。待ち続けると
クライアント側のタイムアウトと二重待ちになり、どちらが原因か画面から読めなくなる。

**2GB 制限のコンテナで検証済み**: 全走査を混ぜた 70 リクエストを 12 並列で
投げて失敗 0 件、ピーク 682MiB (33%)、`OOMKilled=false` (量子化前の測定)。

v5 のイメージ内実測 (core / 2GB 制限、`docker run -m 2g`):

| | RssAnon | RssFile |
|---|---|---|
| store を開いた直後 | 19MB | 19MB |
| 検索 20 回 | 69MB | 130MB |
| 全走査 + compose まで | **83MB** | 148MB |

**イメージは 641MB。** 内訳は venv 362MB (うち sudachidict-core 208MB) +
索引 148MB + Python の基底 36MB。推移は ANN のグラフを焼いていた頃が 2.77GB、
グラフをやめて 1.75GB、int8 量子化で 329MB、その後 `sudachidict-full` が
依存に入って 1GB まで戻り、**v5 で索引を半分にして full 辞書を任意依存に
外して 641MB**。

**残りの主役は Sudachi の core 辞書 (208MB) で、索引 (148MB) より大きい。**
読みの取得に要るので外せないが、イメージをさらに削るならここが次の的になる。

**メモリを 4GB にはできない。** shared-cpu-1x の上限が 2048 MiB で、`memory = '4gb'`
にすると `invalid config.guest.memory_mb, cannot exceed 2048 MiB` でデプロイが
落ちる (このとき**既に動いているマシンはそのまま残る**ので、サービスは停止しない)。
増やすには CPU を上げる必要がある。

**門のテストは sleep では書けない。** `sample_store` は 18 語しかないので走査が
一瞬で終わり、上限を緩めても同時に入らない。`threading.Barrier` で全員が揃うまで
中で待たせ、**「門が無ければ peak == workers になる」ことを利用して
`peak < workers` を主張する** (`test_web.py::test_mora_scan_concurrency_is_capped`)。
上限を 16 に緩めると `assert 8 < 8` で落ちることを確認してある。

**0 台に落とさない** (`auto_stop_machines = "suspend"` + `min_machines_running = 1`)。
停止すると次のリクエストが索引ロードを払う (core 1.0 秒 / full 9.6 秒)。

**v5 デプロイ後の本番実測 (nrt / core)**:

| | v4 | v5 |
|---|---|---|
| `MemAvailable` | 949MB | **1699MB** |
| `Cached` | 997MB | 225MB |
| プロセスの RssAnon / RssFile | — | **107MB / 143MB** |
| 検索 | 33〜56ms | 22ms |
| `phrase` | 371ms | 432ms |

**空きメモリが 750MB 増えた。** `Cached` が減っているのは索引が 274MB -> 148MB に
なってページキャッシュに載る量そのものが減ったため (mmap は必要なページしか
読まないので、これは性能低下ではない)。`min_machines_running = 1` が効いているので
通常運用でコールドスタートは起きない。

**全マシンを手で suspend すると復帰の初回が 48 秒になる。** これは索引ロードでは
なく Fly のプロキシが `machine was recently stopped and is unavailable to service
request` のマシンにルーティングを試みて待つ時間で、ログを見ると起動自体は 158ms
しかかかっていない。`min_machines_running = 1` があるので通常は起きない経路だが、
**計測のために全台を止めると人工的にこの状態を作れてしまう**ので、コールドスタートを
測るときは数値の出どころをログで確かめる。

## 言語とスタイル

コード内のドキュメンテーション文字列・コメントは日本語で書く。既存コードは「なぜこの
選択をしたのか」「何を試して駄目だったのか」を実測値つきで残すスタイルなので、これに
合わせる。ruff の `RUF001-003` (全角記号を ASCII の誤入力として警告) は無効にしてある。
